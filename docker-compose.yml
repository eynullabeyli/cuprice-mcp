services:
  cuprice-mcp:
    container_name: cuprice-mcp
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      PORT: 5000
      CUPRICE_BASE: "${CUPRICE_BASE:-https://app.cuprice.io}"
    ports:
      - "5000:5000"
    restart: unless-stopped
